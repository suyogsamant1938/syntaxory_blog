{
    "private": true,
    "workspaces": [
        "client",
        "server"
    ],
    "scripts": {
        "build": "npm run build --workspace=client && cp -r client/dist ./dist"
    },
    "engines": {
        "node": "20.x"
    }
}